# Rails ActiveRecord

## Conventions
- rails generate migration AddFieldToTable
- change method (reversible by default)
- Separate migrations for schema changes and data
- Strong migrations gem for safety checks
