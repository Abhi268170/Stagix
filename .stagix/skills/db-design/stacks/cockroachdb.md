# CockroachDB

## Specifics
- PostgreSQL wire compatible (use pg drivers)
- UUID primary keys (distributed-friendly)
- SERIALIZABLE isolation by default
- Multi-region: configure locality for data residency
- Import/export via IMPORT INTO and EXPORT
