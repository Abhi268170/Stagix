# GitLab CI

## Conventions
- Stages: lint, test, build, deploy
- Cache for dependencies (node_modules, .venv)
- Rules for conditional jobs
- Environments for deploy tracking
- Include for shared pipeline templates
