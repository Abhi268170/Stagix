# Python Django Standards

## Conventions
- Class-based views for CRUD, function views for custom logic
- Django REST Framework serializers for API validation
- Model managers for complex querysets
- Signals sparingly (prefer explicit service calls)
- Django migrations (never raw SQL for schema changes)
- pytest-django for testing
